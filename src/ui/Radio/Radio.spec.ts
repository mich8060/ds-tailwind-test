export const RADIO_DEFAULTS = {
  className: "",
} as const;

export const RADIO_STORY_SPEC = {
  defaults: {
    className: RADIO_DEFAULTS.className,
  },
  stories: {
    default: {},
  },
} as const;
