export const MICRO_CALENDAR_DEFAULTS = {
  className: "",
} as const;

export const MICRO_CALENDAR_STORY_SPEC = {
  defaults: {
    className: MICRO_CALENDAR_DEFAULTS.className,
  },
  stories: {
    default: {},
  },
} as const;
