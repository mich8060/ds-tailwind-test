export const DIVIDER_DEFAULTS = {
  className: "",
} as const;

export const DIVIDER_STORY_SPEC = {
  defaults: {
    className: DIVIDER_DEFAULTS.className,
  },
  stories: {
    default: {},
  },
} as const;
