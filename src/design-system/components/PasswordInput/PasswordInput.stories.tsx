import React from "react";
import { PasswordInput } from "./PasswordInput";

export default { title: "Components/PasswordInput", component: PasswordInput };

export const Default = {
  args: {
    children: "PasswordInput"
  }
};
