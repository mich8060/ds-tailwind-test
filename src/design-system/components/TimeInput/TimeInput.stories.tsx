import React from "react";
import { TimeInput } from "./TimeInput";

export default { title: "Components/TimeInput", component: TimeInput };

export const Default = {
  args: {
    children: "TimeInput"
  }
};
