import React from "react";
import { CurrencyInput } from "./CurrencyInput";

export default { title: "Components/CurrencyInput", component: CurrencyInput };

export const Default = {
  args: {
    children: "CurrencyInput"
  }
};
