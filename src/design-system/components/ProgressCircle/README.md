# ProgressCircle

## Structure Compliance

- Uses semantic token styles from `var(--uds-...)`.
- BEM naming with `.uds-progress-circle` as the block prefix.
- Variants and defaults are defined in `ProgressCircle.spec.ts`.
- Public exports are limited to component and types through `index.ts`.
