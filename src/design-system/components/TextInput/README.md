# TextInput

## Structure Compliance

- Uses semantic token styles from `var(--uds-...)`.
- BEM naming with `.uds-text-input` as the block prefix.
- Variants and defaults are defined in `TextInput.spec.ts`.
- Public exports are limited to component and types through `index.ts`.
