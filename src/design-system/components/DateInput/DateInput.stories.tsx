import React from "react";
import { DateInput } from "./DateInput";

export default { title: "Components/DateInput", component: DateInput };

export const Default = {
  args: {
    children: "DateInput"
  }
};
