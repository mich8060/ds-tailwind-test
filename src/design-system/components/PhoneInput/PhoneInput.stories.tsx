import React from "react";
import { PhoneInput } from "./PhoneInput";

export default { title: "Components/PhoneInput", component: PhoneInput };

export const Default = {
  args: {
    children: "PhoneInput"
  }
};
