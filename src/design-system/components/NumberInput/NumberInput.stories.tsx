import React from "react";
import { NumberInput } from "./NumberInput";

export default { title: "Components/NumberInput", component: NumberInput };

export const Default = {
  args: {
    placeholder: "Enter a number"
  }
};
