import type { ComponentSpec } from "../../specs/spec.types";

export const DateRangeInputSpec: ComponentSpec = {
  name: "DateRangeInput",
  tier: 2,
  purpose: "Describe purpose.",
  variants: {},
  states: ["default"],
  tokensUsed: [],
  accessibility: { role: "generic", keyboard: [] }
};
