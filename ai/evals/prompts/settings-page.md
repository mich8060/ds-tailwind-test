Create a settings page for notification routing inside the Unified Design System package contract.

Requirements:
- keep the page inside `AppShell`
- use exported field and input primitives
- avoid raw form chrome when exported components exist
- keep imports on the package root only
