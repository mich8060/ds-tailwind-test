# Expected signals

- `showListview={true}` is used
- the `listview` slot contains list rows built from `Item` or similar exported UDS surfaces
- detail content stays in `children`, not in an ad hoc parallel layout
